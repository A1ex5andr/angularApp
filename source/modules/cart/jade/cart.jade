.container.cart(data-ng-controller="CartCtrl as inCart")
    h2 Shopping Cart
    table.table
        tr
            th
            th Model
            th Name
            th Qty
            th Cost

        tr(data-ng-repeat='product in inCart.products')
            td.col-md-2
                img.img-responsive(data-ng-src="{{product.images[1]}}")
            td
                div {{ product.name }}
            td
                div {{ product.model }}

            td
                input.input-mini(type='number', value='1')
            td
                div {{ product.price }}